# Used by docker-compose.yml (docker-compose reads a .env file at the project
# root automatically). Copy this to `.env` in the project root and fill in
# real values before running `docker compose up`.

CLIENT_URL=http://localhost:8080
JWT_SECRET=change_this_to_a_long_random_secret
RAZORPAY_KEY_ID=rzp_test_TJmEQE9WLUlIpX
RAZORPAY_KEY_SECRET=50L55xRFGklWHQCR1C0PlhpV

SEED_ADMIN_EMAIL=admin@rakhi.com
SEED_ADMIN_PASSWORD=Admin@123

# Optional — outbound email for order invoices
SMTP_HOST=
SMTP_PORT=587
SMTP_SECURE=false
SMTP_USER=
SMTP_PASS=
SMTP_FROM=

# Optional — S3-compatible file storage (recommended if you deploy the
# backend anywhere without a persistent volume for backend/uploads)
S3_BUCKET=
S3_REGION=
S3_ACCESS_KEY_ID=
S3_SECRET_ACCESS_KEY=
S3_ENDPOINT=
S3_PUBLIC_URL_BASE=
